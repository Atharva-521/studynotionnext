import React from 'react'

export const Card = ({heading, content, footerLeft, footerRight, footerLeftIcon, footerRightIcon}: {heading: string, content: string, footerLeft: string, footerRight: string, footerLeftIcon: React.ReactNode, footerRightIcon: React.ReactNode}) => {
  return (
    <div>Card</div>
  )
}
